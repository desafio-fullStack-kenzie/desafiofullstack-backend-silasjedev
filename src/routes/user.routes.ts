import { Router } from "express";

const userRoutes = Router();

userRoutes.post("")
userRoutes.get("")
userRoutes.patch("/:id")
userRoutes.delete("/:id")
userRoutes.patch("/:id/image")

export default userRoutes;